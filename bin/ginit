#!/usr/bin/env bash

[[ $# -eq 0 ]] && { echo "${0##*/} requires an argument."; exit 1; }

case $1 in
	bspwm ) exec xinit bspwm ;;
	herbstluftwm|herbstluft|hlwm ) exec xinit herbstluftwm ;;
	sway ) "${HOME}/.autostart" wayland & sway ;;
	* ) echo "No entry for $1." ;;
esac
