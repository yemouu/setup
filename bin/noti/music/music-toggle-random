#!/usr/bin/env bash

app_name="MPD Toggle Random"
summary="Random: $(mpc random | awk NR==3'{print $6}')"
body="$(mpc -q current)"

dunstify -a "$app_name" \
	-t 4000 \
	-r 1 \
	"$summary" \
	"$body"
