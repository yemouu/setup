#!/usr/bin/env bash

app_name="MPD Seek"

[[ $1 =~ [+-]?[0-9][0-9]% ]] && percentage="$1" || exit 1

summary="$(mpc seek "$percentage" | awk NR==2'{print $3}') ($percentage)"
body="$(mpc -q current)"

dunstify -a "$app_name" \
	-t 4000 \
	-r 1 \
	"$summary" \
	"$body"
