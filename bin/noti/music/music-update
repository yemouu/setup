#!/usr/bin/env bash

app_name="MPD Update"
summary="Music Queue Updated"

mpc -q rescan
mpc -q clear
mpc -q listall | mpc add
mpc toggle
sleep .01

body="$(mpc current)"

dunstify -a "$app_name" \
	-t 4000 \
	-r 1 \
	"$summary" \
	"$body"
