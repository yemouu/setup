#!/bin/sh

usage() {
    printf '%s\n' "usage: ${0##*/} [TIMEZONE]\n" \
	   "Availavle timezones can be found at" \
	   "/usr/share/zoneinfo/\n"
}

case $1 in
    -h|--help ) useage; exit 0 ;;
esac

[ -n "$1" ] && export TZ="$1"

app_name="Time and Date"
summary="$(date +'%R %d %a %b %Y') $1"

notify-send -a "$app_name" \
	    -t 4000 \
	    "$summary"
